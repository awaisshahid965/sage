# Copy to `.env` and adjust. `.env` is gitignored; this file is not.
# Every variable is prefixed SAGE_ and maps to a field on sage.config.Settings.

SAGE_ENVIRONMENT=local
SAGE_DEBUG=true

SAGE_HOST=127.0.0.1
SAGE_PORT=8000

SAGE_LOG_LEVEL=INFO
SAGE_LOG_JSON=false
