"""Strategies that implement the `sage.domain.context.ContextStrategy` port.

One module per family of technique. `history.py` works from the conversation
itself; retrieval from documents lands beside it when it arrives.
"""
